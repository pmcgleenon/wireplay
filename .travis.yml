before_install: 
    - sudo apt-get update -qq
    - sudo apt-get install -y libpcap-dev 
    - sudo apt-get install -y libnet

language: c

os: linux

compiler: gcc

env:
    - AUTOTOOLS=no COVERAGE=no BUILD=static

script:     make all
